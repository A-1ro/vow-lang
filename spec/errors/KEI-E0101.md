# KEI-E0101: unexpected token

構文解析: 文法上その位置に置けないトークンが現れた。閉じ括弧・コロン・
カンマ等の欠落が典型で、その場合は欠落トークンの挿入が fix として提示される。

```kei
func add(a: Int, b: Int -> Int {   // error: expected ',' or ')', found '->'
```

## 修正

メッセージの `expected ...` に従い、欠落しているトークンを補う。
