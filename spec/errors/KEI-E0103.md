# KEI-E0103: unclosed delimiter

構文解析: `{` `(` などの開きデリミタに対応する閉じデリミタが
ファイル末尾までに見つからなかった。span は開きデリミタの位置を指す。

```kei
func clamp(n: Int) -> Int {
  if n > 100 {
    return 100
}                  // error: 関数本体の '}' が足りない
```

## 修正

対応する閉じデリミタを補う(fix は EOF 位置への挿入を提案する)。
