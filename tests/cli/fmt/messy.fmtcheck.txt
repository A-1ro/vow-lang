tests/cli/fmt/messy.input.kei: not formatted (run `kei fmt --write tests/cli/fmt/messy.input.kei`)
 module cli.messy
 
 func calc(a: Int, b: Int, c: Int) -> Int {
-    let x = ((a) + (b * c))
-    let y = (a + b) * c
-    let z = ((a + b)) * (c)
-    return -((x) - (y / z))
+  let x = a + b * c
+  let y = (a + b) * c
+  let z = (a + b) * c
+  return -(x - y / z)
 }
